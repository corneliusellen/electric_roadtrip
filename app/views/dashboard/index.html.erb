<% if current_user %>
  <h2>Hello, <%= "#{current_user.first_name} #{current_user.last_name}" %></h2>
  <p><%= link_to "Logout", logout_path %></p>
  <p>Vehicles:</p>
  <!-- <% if current_user.vehicles.empty? %> -->
    <%= link_to "Add First Vehicle", new_vehicle_path %>
  <!-- <% else %>
    <% current_user.vehicles.each do |vehicle| %>
      <%= vehicle.name %>
    <% end %> -->
    <!-- <%= link_to "Add Vehicle", new_vehicle_path %> -->
  <!-- <% end %> -->
<% end %>
